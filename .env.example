# Copy to `.env` in the project root (do not commit `.env`).
# Create a project at https://supabase.com and paste URL + anon key from Settings → API.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
