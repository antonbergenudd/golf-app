/**
 * Loaded in `app/_layout.tsx` via `@expo-google-fonts/roboto` — matches Flutter
 * Material typography (Roboto) for consistent weight + spacing vs system UI fonts.
 */
export const Font = {
  regular: "Roboto_400Regular",
  medium: "Roboto_500Medium",
  semiBold: "Roboto_600SemiBold",
  bold: "Roboto_700Bold",
  black: "Roboto_900Black",
} as const;
